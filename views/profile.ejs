<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>User Profile</title>
  <style>
    /* Resetting some default browser styles */
    * {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
    }

    /* Body settings */
    body {
      font-family: 'MedievalSharp', cursive;
      height: 100vh;
      width: 100%;
      display: grid;
      grid-template-rows: 1fr 12fr;
      grid-template-columns: 100%;
      overflow: hidden;
      color: #fff;
    }

    /* Main container setup */
    .maincont {
      width: 500px;
      height: 500px;
      display: grid;
      grid-template-columns: 1fr 1fr;
      grid-template-rows: 100%;
      position: relative;
      top: 50%;
      left: 50%;
      transform: translate(-50%, -50%);
      border-radius: 10px;
      box-shadow: 0 4px 10px rgba(0, 0, 0, 0.566);
    }

    /* Left side: Profile card */
    .left {
      position: relative;
      height: 100%;
      width: 100%;
      display: flex;
      justify-content: center;
      align-items: center;
    }

    /* Right side: Edit forms */
    .right {
      background: #001380;
      height: 100%;
      width: 100%;
      background-color: #002199;
      padding: 20px;
      display: flex;
      flex-direction: column;
      justify-content: space-evenly;
      overflow-y: auto;
    }

    /* Profile card (on the left) */
    .profile-card {
      height: 70%;
      text-align: center;
      padding: 20px;
      background-color: #ffffff;
      border-radius: 10px;
      color: #000;
      box-shadow: 0 0 10px rgba(0, 0, 0, 0.478);
      width: 80%;
    }

    /* Heading styles */
    h2 {
      margin: 10px 0;
      font-size: 1.5em;
      color: #007BFF;
    }

    p {
      margin: 5px 0;
      font-size: 1em;
      color: #000000;
    }

    /* Form and labels */
    form h3 {
      margin-bottom: 10px;
      font-size: 1.2em;
      color: #ffffff;
    }

    label {
      display: block;
      margin-top: 10px;
      font-size: 0.9em;
    }

    /* Input fields */
    input {
      width: 100%;
      padding: 10px;
      margin-top: 5px;
      border-radius: 5px;
      border: 2px solid #3d3d3d;
      background: #ffffff;
      color: #000000;
      font-size: 0.95em;
    }

    input::placeholder {
      color: #464545;
    }

    /* Submit button */
    .update-btn {
      margin-top: 20px;
      padding: 12px;
      background-color: #000e4b;
      color: #ffffff;
      border: none;
      border-radius: 5px;
      width: 100%;
      font-weight: bold;
      cursor: pointer;
      transition: background-color 0.3s;
      border: 3px solid rgb(255, 255, 255);
    }

    /* Button hover effect */
    .update-btn:hover {
      background-color: #002853;
      border: 3px solid rgb(255, 255, 255);
    }

    /* Success message styling */
    .success-message {
      color: limegreen;
      font-size: 0.9em;
      margin: 5px 0 10px 0;
    }

    /* Error message styling */
    .error {
      color: red;
      font-size: 0.85em;
      margin-top: 5px;
    }

    /* Media query for smaller devices */
    @media (max-width: 768px) {
      .maincont {
        width: 90%;
        height: auto;
        display: flex;
        flex-direction: column;
        justify-content: center;
      }

      .left,
      .right {
        width: 100%;
        height: auto;
      }
    }
  </style>
</head>

<body>
  <!-- Header (optional inclusion) -->
  <%- include('partials/header') -%>

    <div class="maincont">
      <!-- Left: Profile Info -->
      <div class="left">
        <div class="profile-card">
          <h2>
            <%= name %>
          </h2>
          <p>Username: <%= username %>
          </p>
          <p>Shop Name: <%= shopname || 'Not available' %>
          </p>
        </div>
      </div>

      <!-- Right: Edit Forms -->
      <div class="right">
        <!-- Edit Name Form -->
        <form action="/profile/name" method="POST" class="edit-account-container">
          <h3>Edit Name</h3>
          <% if (successName) { %>
            <p class="success-message">
              <%= successName %>
            </p>
            <% } %>
              <label for="name">Name</label>
              <input type="text" id="name" name="name" placeholder="Enter your name" required />
              <button class="update-btn" type="submit">Update Name</button>
        </form>

        <!-- Edit Username Form -->
        <form action="/profile/username" method="POST" class="edit-account-container">
          <h3>Edit Username</h3>
          <% if (successUsername) { %>
            <p class="success-message">
              <%= successUsername %>
            </p>
            <% } %>
              <label for="username">Username</label>
              <input type="text" id="username" name="username" placeholder="Enter new username" required
                minlength="8" />
              <button class="update-btn" type="submit">Update Username</button>
        </form>
      </div>
    </div>
</body>

</html>